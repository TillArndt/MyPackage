[General]
maxNumProcesses=3 # number of parallelly executed cmsRun processes
enableByDefault=True #if enalbe is not set in a specific section, this is applied.
stackedHistos=histogramName, otherName # Histograms to be stacked
cutflowParsePaths=path, otherPath 
cutflowParseModules=module, otherModule
cfgFile=MyPackage.TtGammaAnalysis.myttbarPatTuple_cfg # python import path!

[abbreviation_for_fileset]
enable=True # optional, see also enableByDefault
isData=True # only needed for histo-stacking (default is false)
lumi=3456.4 # only needed for histo-stacking
legend="Data" # only needed for histo-stacking
extraCode=\
#(optional) \n\
#this is appended to your conf. \n\
#you could do things like: \n\
#process.path.replace( ..., ...)
eventOutput=/output/path/for/outputmodule.root # only needed if output module defined
eventInput\dir=/folder/for/all/input/files 
eventInput\files=file:inputfile.root,file:other/inputfile.root

# this would be the minimum to be specified for a dataset 
[fileset_abbreviation] 
eventInput\files=file:inputfile.root,file:other/inputfile.root
